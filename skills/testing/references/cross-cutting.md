## 4. Cross-Cutting (Both Tracks)

- **Negative/errors/network (mocks).** Cover every status branch: 400 (validation→fields)/401 (redirect to login, return URL)/403/404/409/422/429 (`Retry-After`)/500/502/503/504 — clear message without a stacktrace, no `undefined`/`[object Object]`, no double display (toast+inline). Body off contract (HTML instead of JSON, truncated JSON, empty body with 200). Timeout (no eternal spinner). Slow network (skeletons, disabling). Offline at the moment of submit (data not lost), recovery, flapping (no cascade of duplicates). Retry (retryable errors only, backoff, idempotency). Degradation without UI crash (error boundary, placeholders, broken image → alt). **Playwright mocks:** `page.route` + `route.fulfill({status})` for every branch; `route.abort('failed')`; artificial delay → loaders; broken body `route.fulfill({body:'<<not json'})`; `context.setOffline(true)`; races (delay the first, release the second); sequential responses (counter: 500→200 for retry); inspect `request.postData()`/headers (idempotency-key, Authorization, duplicates); CDP `Network.emulateNetworkConditions` for a realistic slow network; remove routes after the test.
- **Integrations (request/response/payload).** For every action capture the HTTP (method/URL/query/headers/body); status matches the semantics; payload per contract (names/types/nesting); UI renders from the response, not from state/mock; one action = one request (no duplicates/N+1); no dangling requests after leaving the page (AbortController). Payload: required fields filled with real values; no extra/debug fields; sensitive data not in query/not in plain text; clientIp/fingerprint not `undefined`/`null`; values masked in the UI sent in full format; money in the correct unit (major vs minor currency units, no float precision loss); dates in TZ/ISO without a ±1 day shift; bool/enum in the backend's format (`true` vs `"true"`); special-character encoding. **UI↔Backend consistency:** what's shown after an operation = what's actually stored (repeat GET); optimistic update rolls back on 4xx/5xx; aggregates/counters match a recount; statuses not hardcoded "Success"; data available on other screens (no cache desync).
- **State/session.** Login creates a session (cookie HttpOnly/Secure/SameSite); logout actually invalidates server-side (Back from bfcache does not serve private data); idle timeout (sliding vs absolute); refresh token (auto-renewal, expired → login); parallel sessions; "remember me"; "sign out of all devices"; F5 on any page (restore/reset without a white screen); reload during an in-flight request (no duplicate POST); beforeunload on unsaved changes; role change/downgrade in an active session (permissions update/403 on a forbidden URL); deactivation kills the session; multi-account does not mix contexts.
- **localStorage/sessionStorage.** sessionStorage isolated per tab, localStorage shared; sensitive data not in plain text; corrupted/deleted keys don't crash the app (invalid JSON → fallback); QuotaExceeded; clearing/private mode (graceful degradation); schema versioning (old format migrated/reset); logout clears user data.
- **Multi-tab.** Logout in one tab → reaction in the others (storage event); login picked up; data change in tab A reflected in tab B; parallel editing of the same entity (optimistic lock, no lost update); Ctrl+click preserves context.
- **Navigation/routing/404/deeplink.** Back/Forward restore state (filters/pagination/tabs/scroll, URL=content); SPA History API without desync; Back after submit does not duplicate; deep link to an entity/tab/filter opens directly; protected page without auth → login + return; nonexistent/deleted resource → 404/empty state, not a white screen; state URL params survive sharing; manual injection of invalid query → defaults; hash/anchors scroll; trailing slash/case/redirects (301/302, loops); `target=_blank`+`rel=noopener`.
- **Time/locale/i18n/TZ.** Dates in user TZ vs server TZ; device TZ change; DST (nonexistent/duplicated hour); day/week/month/year boundary ("today" filter respects TZ); clock skew (tokens/"5 minutes ago" not in the future); Feb 29/leap years; midnight UTC vs local (no day-shift); very old/future dates. Locale switch changes all strings (no raw i18n keys); date/time/number/currency formats per locale; plural rules; long translations don't break the layout; RTL mirroring; variable interpolation; locale affects sorting (collation) and casing.
- **Non-functional/performance/console.** Console open for the entire run: distinguish `error`/`warning`, capture the environment's noise baseline (CDN/analytics/extensions), separate noise from bugs (only on interaction with the feature under test, application domain, by timestamp); `Uncaught TypeError ... undefined` after an API call = unguarded render; React keys/hydration mismatch; CSP/CORS/mixed content; reproduce in incognito without extensions. Core Web Vitals (LCP<2.5s, CLS<0.1, INP/TBT) with mobile emulation + CPU 4x + Slow 4G; CLS pitfalls (images/iframes without `width/height`, late font, cookie banner on top); heavy resources (uncompressed images, no lazy loading); bundles (size, code-splitting); gzip/brotli; static asset caching; request waterfall/N+1; virtualization of long lists; animations via transform/opacity (not width/top). Memory leaks: open-close a modal/route 20–50 times → heap snapshot (detached DOM, uncleared setInterval/timers/listeners/WebSocket); long session 30–60 min (heap does not grow linearly); background tab (timer throttling).
- **Security cross-cutting (from the frontend).** XSS in every field (`<script>alert(1)</script>`, `<img src=x onerror=>`, `"><svg onload=>`) — save, reload, check EVERY output location (header, list, preview, PDF, title); HTML injection without script; attribute context (`javascript:` in href); rich-text/markdown sanitization; DOM-based XSS from URL/hash; SVG/double-extension on upload; template injection (`{{7*7}}`). Transport/headers: sensitive data not in the URL (logs/Referer); no tokens/PII in `console.log` in production; tokens in localStorage (XSS) vs httpOnly cookie; cookie flags; all traffic HTTPS, no mixed content; HSTS; security headers; CORS not `*` with credentials. Access/IDOR/CSRF: direct link to private content in incognito → login; access after logout (Back/replay); IDOR (swap `id`/`userId` to someone else's); vertical privilege (admin endpoint as a regular user — 403, not just a hidden button); protection that lives only in the UI (replay the request from Network); direct links to files/exports; CSRF (anti-CSRF token/SameSite); parameter tampering (`price`/`discount`/`isAdmin` in the body); mass assignment; brute force/rate limit; user enumeration; open redirect (`?redirect=`/`?next=` to an external domain); duplicate payment submission protection.
- **Analytics/metric events.** Events sent (clicks/views/conversions) with the correct payload; no duplicates; events on errors; GDPR mode (only after consent); works with tracker blockers (no crashes); no PII/tokens leaking to third parties.
- **Files/uploads/export.** Upload: formats by extension AND MIME/signature, extension spoofing; limits (0 bytes, exactly at the limit, +1, huge); multiple files, duplicate names, Cyrillic/emoji/long filename, `../` path; interruption/resume/progress/cancel; drag&drop vs button vs Ctrl+V; corrupted/polyglot/zip bomb; preview, delete/replace. Download: name/extension/content, resume, parallel downloads. Print (Ctrl+P): print styles, page breaks, hiding non-content. Export CSV/XLSX/PDF: matches on-screen data, UTF-8 BOM, delimiters, **CSV injection (`=`/`+`/`-`/`@` at cell start)**, empty/large dataset, with filters applied, number/date locale.
- **Search/filters/sorting.** Search: empty/1 character/partial/case-insensitivity/typos/transliteration; special characters and operators, whitespace, long query; highlighting, debounce, cancellation of stale requests (race); "nothing found" empty state, reset. Filters: combinations, reset all, persistence in URL, conflicting filters, counter. Sorting: by every field, asc/desc, stability, with null/empty values, persistence across pagination.
- **Drag&drop/interactive.** Valid/invalid zones, cancel (Esc), drop outside a zone; list reordering + persistence after reload; touch equivalent + autoscroll at the edge; panel/column resize + persistence.
- **Undo/redo/reversibility.** Ctrl+Z/Y, history depth; undo snackbar for "delete"; bulk operations and rollback; autosave vs manual save, unsaved-changes indicator, loss on leaving.
- **Scroll/pagination/virtualization.** Infinite scroll (loading, no duplicates/gaps at page seams, end of list); virtualization under fast scrolling + position kept on return (scroll restoration on Back); pagination (first/last page, direct URL to a page, page size); sticky header/"back to top".
- **Feature interaction/regression.** New feature interacting with adjacent ones (several enabled simultaneously); regression of affected areas; feature flags (on/off/partial rollout/A/B — stable branch per user); state conflicts (two features in one store/URL/localStorage); backward compatibility with old data/links/bookmarks; migrations (old records after deploy, defaults for new fields, old Storage structure → upgrade/reset, client↔backend version desync, SW cache after release).
- **Consents/legal texts.** Cookie banner (accept/decline/customize, choice remembered, no tracking before consent); consent checkboxes (terms of service/personal data/newsletter — required flags, links to documents, cannot submit without the required ones); age/region restrictions, version of accepted terms, re-prompt on change.
- **Captcha/rate limit (UI).** Captcha appears on trigger (N failed attempts), passing it, captcha error/reload, submission blocked until passed, accessibility, behavior when the provider is unavailable; rate limit (login/code/search — hitting the limit, message, cooldown timer, 429 + backoff); double-click protection (idempotency, button lockout).
- **Payments and money (if applicable).** Rounding/minor units/negative/zero/limits/currency and currency change; 3DS redirect to the bank and back, payment timeout, double charge, cancellation/refund; promo codes (application/expiry/combining/invalid code); OTP/SMS (code request, resend timer, attempt limit, expiry, wrong code, code not in the response/logs); 3-D Secure (pass/decline/timeout, final order status); PCI DSS (no card data on your own backend, tokenization).
- **Webhooks/callbacks (from the frontend).** Status from webhook/polling reaches the UI without a manual refresh; polling with a limit/backoff; late/duplicated webhook does not break the status; redirect after an external callback (3DS/OAuth) lands on the right page; lost callback (tab closed) — the operation completes, status visible on return.
- **Interruptions/environment resilience.** visibilitychange (tab minimized/restored), wake from sleep, focus loss; screen rotation mid-operation, incoming call; wizard interrupted midway (resume from the right step); behavior with cookies/localStorage/JS disabled, incognito.
- **Email/notifications.** Email triggered by the event, exactly once (no duplicates), delay; content (data, working links, token lifetime, unsubscribe); localization by language, From/Reply-To, subject; rendering in Gmail/Outlook/mobile, plain-text fallback; push/in-app/toast (appearance, auto-hide, stacking, permissions, opt-out); notification bell (unread counter, "mark as read", tab sync).
